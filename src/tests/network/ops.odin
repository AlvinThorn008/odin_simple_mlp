#+private
package network_test

import mat "../../mat"
import nn "../../network"

import "core:testing"
import "core:log"

SMat :: mat.SMat

@(test)
broadcast_add_column_vecs :: proc(t: ^testing.T) {
}
